import React from 'react';
import {Text, View} from 'react-native';

const NowPlaying: React.FC = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{color: 'white'}}>NowPlaying</Text>
    </View>
  );
};

export default NowPlaying;
