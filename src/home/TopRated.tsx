import React from 'react';
import {Text, View} from 'react-native';

const TopRated: React.FC = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{color: 'white'}}>TopRated</Text>
    </View>
  );
};

export default TopRated;
